CREATE TABLE series(
	idSerie int identity (1, 1) NOT NULL,
	titulo VARCHAR(40) UNIQUE NOT NULL,
	genero VARCHAR(25) NOT NULL,
	temporadas int NOT NULL,
	emissora VARCHAR(35) NOT NULL,
	sinopse VARCHAR(999) NOT NULL,
	imagem VARCHAR(999) NOT NULL,
	
	PRIMARY KEY (idSerie)
)